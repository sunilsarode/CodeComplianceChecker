
#include <stdio.h>
#include <stdlib.h>

int main()
{

    int x, y;
    scanf("%d", &y);
    scanf("%d", &x);
    switch (x)
    {

    case 1:

        switch (y)
        {

        case 1:
            printf("%d", 1);
            while (0)
            {
            case 6:
                x++;
                break;
            }
            //break;
        case 2:
            if (y)
            {

            default:
                x++;
            }
            //break;

        
            do
            {
            case 7:
                break;
            } while (x--);
            x++;
            // break;
        }
        //break;
    default:
        x++;    

    case 4:
        printf("%d", 1);
        if (y)
        {

        case 5:
            x++;
        }
        break;

   
    }
    return 0;
}